window.onload = function() {
    alert('Selamat datang di website Rchmad Rmadhan!');
    confirm("Apakah Anda ingin melanjutkan?");
    prompt("Saya Belajar:");
    
    // Contoh penggunaan Date dan parseInt
    const tanggal = new Date(10);
    const angka = parseInt("123");
    
    // Menampilkan pesan setelah 2 detik
    setTimeout(function() {
        alert('Pesan 2 detik lagi');
    }, 2000);
    // Menampilkan pesan setelah 2 detik
    setTimeout(function() {
        alert('Pesan 3 detik lagi');
    }, 2000);
    // Menampilkan pesan setelah 2 detik
    setTimeout(function() {
        alert('Pesan 4 detik lagi');
    }, 2000);
    // Menampilkan pesan setelah 2 detik
    setTimeout(function() {
        alert('Pesan 5 detik lagi');
    }, 2000);
};